pipeline {
    agent any
    tools {
        maven 'Maven'
    }
    stages {
        stage ('Initialize') {
            steps {
                sh '''
                    echo "PATH = ${PATH}"
                    echo "M2_HOME = ${M2_HOME}"
                    '''
            }
        }
        stage ('Build') {
            steps {
                sh 'mvn clean package'
            }
        }

       stage ('Deploy_to_tomcat') {
          steps {
        sshagent(['tomcat']) {
          sh 'scp -o StrictHostChekingKey=no traget/*.war ubuntu@34.125.86.238:/home/iamonaws2024/tomcat/apache-tomcat-10.1.19/webapps/webapp.war'
         }
      }
 }
}
}

