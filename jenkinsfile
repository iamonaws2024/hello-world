pipeline {
    agent any
    tools {
        maven 'Maven'
    }
    stages {
        stage ('Initialize') {
            steps {
                sh '''
                    echo "PATH = ${PATH}"
                    echo "M2_HOME = ${M2_HOME}"
                    '''
            }
        }
        stage ('Build') {
            steps {
                sh 'mvn clean package'
            }
        }

       stage ('Deploy_to_tomcat') {
          steps {
        sshagent(['tomcat']) {
          sh 'scp -o StrictHostKeyChecking=no traget/*.war ubuntu@10.182.0.13:/home/iamonaws2024/apache-tomcat-10.1.19/webapps/webapp.war'
         }
      }
 }
}
}

